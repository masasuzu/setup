# vim:ft=zsh:

# fix $HOST
case "${HOST/.*/}" in
philippe|henry)
    DOMAIN="[ LOCAL(l) ]"
    ;;
leon)
    DOMAIN="[ INTRA ]"
    ;;
lenny*|squeeze*)
    DOMAIN="[ LAB ]"
    ;;
lab)
    DOMAIN="[ VPS-LAB ]"
    ;;
*)
    print ".zsh/devprofile: $HOST: unkown hostname"
    ;;
esac
export DOMAIN
