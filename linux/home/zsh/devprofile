# fix $HOST
case "${HOST/.*/}" in
philippe)
    DOMAIN="[ LOCAL ]"
    ;;
leon)
    DOMAIN="[ INTRA ]"
    ;;
lenny*|squeeze*)
    DOMAIN="[ LAB ]"
    ;;
*)
    print ".zsh/devprofile: $HOST: unkown hostname"
    ;;
esac
export DOMAIN
