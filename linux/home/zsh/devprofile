# vim:ft=zsh:

# fix $HOST
case "${HOST/.*/}" in
philippe|henry)
    DOMAIN="[ LOCAL ]"
    ;;
leon)
    DOMAIN="[ INTRA ]"
    ;;
lenny*|squeeze*)
    DOMAIN="[ LAB ]"
    ;;
lab)
    DOMAIN="[ VPS-LAB ]"
    ;;
*)
    print ".zsh/devprofile: $HOST: unkown hostname"
    DOMAIN="[ UNKNOWN ]"
    ;;
esac
export DOMAIN
